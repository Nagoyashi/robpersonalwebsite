# v0.0.0-rc.1 — release pipeline smoke-test (throwaway)

This is a **throwaway pre-release** created only to verify that `release.yml`
publishes a GitHub Release from `docs/releases/<tag>.md`. It will be deleted.

- Confirms: pushing a `v*` tag → a GitHub Release is published from this file.
- The hyphen in the tag marks this as a pre-release (never "latest").
- No milestone named `v0.0.0-rc.1` exists, so the close-milestone step no-ops.
